#pragma once

// ============================================================================
// Kopiere diese Datei nach "config.h" (wird von Git ignoriert, siehe .gitignore).
//
// Laufzeitkonfiguration (Netzwerk, Passwoerter, Sensor2, Syslog-Server, ...)
// kommt gemaess docs/lastenheft.txt aus config.xml auf LittleFS (siehe
// ConfigManager) - NICHT aus dieser Datei. Hier stehen nur Werte, die zur
// Kompilierzeit feststehen muessen.
// ============================================================================

#define DEVICE_FIRMWARE_VERSION "0.9.0-rc4"

// Projekt-Kennung fuer die OTA-Herkunftspruefung (siehe OtaManager.h/.cpp) -
// eindeutig je Sensormeter-Projekt, damit sich z.B. die .bin von Sensormeter
// Display nicht versehentlich auf diesem Geraet installieren laesst.
#define FIRMWARE_PROJECT_ID "SENSORMETER-POE"
